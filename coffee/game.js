// Generated by CoffeeScript 1.7.1
var canvasEdgeX, canvasEdgeY, endGame, frameRate, gameLoop, gameLoopCounter, interval, levelOver, menuCode, removeMenus, runLevel, setSizes, startLoop;

canvasEdgeX = $(window).width();

canvasEdgeY = $(window).height();

frameRate = 1;

interval = 1000 / frameRate;

levelOver = false;

gameLoopCounter = 0;

menuCode = "<div id='startMenu'> <p>Hello game, this is game.</p> <a href='#' class='menuLinkOne'>Start level one.</a> </div>";

setSizes = function() {
  console.log("setSizes runs");
  canvasEdgeX = $(window).width();
  canvasEdgeY = $(window).height();
  canvas.width = canvasEdgeX;
  return canvas.height = canvasEdgeY;
};

removeMenus = function() {
  console.log("removeMenus runs");
  return $("#startMenu").remove();
};

startLoop = function() {
  var loopId;
  console.log("startLoop runs");
  console.log("levelOver =", levelOver);
  loopId = function() {
    return setInterval(gameLoop(interval));
  };
  return loopId();
};

gameLoop = function() {
  console.log("gameLoop runs");
  console.log("levelOver =", levelOver);
  if (levelOver === true) {
    endGame();
  }
  console.log("levelOver =", levelOver);
  gameLoopCounter += 1;
  return console.log("gameLoopCounter =", gameLoopCounter);
};

endGame = function() {
  console.log("endGame runs");
  clearInterval(loopId);
  return $('#menu').append(function() {
    return menuCode;
  });
};

runLevel = function(levelName) {
  console.log("runLevel runs");
  removeMenus();
  return startLoop();
};

jQuery(function() {
  var canvas;
  canvas = document.getElementById("canvas");
  setSizes();
  $('#menu').append(function() {
    return menuCode;
  });
  return $('.menuLinkOne').on('click', function() {
    return runLevel(100);
  });
});
